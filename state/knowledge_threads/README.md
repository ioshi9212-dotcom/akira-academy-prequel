# Knowledge threads — trigger-load only

These files are not runtime memory. They are thematic/hidden/future archives that must be loaded only when a specific scene trigger requires them.

Normal arrival / court / registration / medcheck scenes must not load these files.
