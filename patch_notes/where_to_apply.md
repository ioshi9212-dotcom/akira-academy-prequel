# Notes

Текущий файл формата сцены в репозитории: `gpt/scene_format.md`.

Патч не заменяет весь файл, а добавляет/уточняет:
- видимую верхнюю шапку без `Рядом`;
- правило активного состояния;
- нижний блок подсказок;
- качество действий, реплик и мыслей.

Почему убрали `Рядом`:
- это спойлер для игрока;
- список nearby/active должен быть скрытым техническим состоянием для движка.
