name: Actions Runtime Smoke Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Disabled experimental smoke workflow
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "Experimental runtime smoke workflow is disabled after soft rollback."
